# FastAPI and Server (Python 3.13: need recent versions for ForwardRef compatibility)
fastapi>=0.115.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# AWS SDK
boto3>=1.42.0
botocore>=1.42.0

# Real Strands Agents SDK (AWS)
strands-agents>=1.20.0
strands-agents-tools>=0.2.0

# Real MCP Server (Model Context Protocol)
# Included as dependency of strands-agents, but listed explicitly
mcp>=1.11.0

# Real Nova Act SDK (Browser Automation)
nova-act>=3.0.0

# Data Validation and Serialization (Python 3.13: use pydantic 2.10+ to avoid ForwardRef._evaluate error)
pydantic>=2.10.0,<3
pydantic-settings>=2.6.0

# CORS and Security
python-jose[cryptography]>=3.3.0
python-dotenv>=1.0.1

# HTTP Client
httpx>=0.26.0

# Date/Time Handling
python-dateutil>=2.8.2

# PDF Report Export (Nova Canvas cover)
reportlab>=4.0.0

# Development
pytest>=7.4.0
pytest-asyncio>=0.23.0
